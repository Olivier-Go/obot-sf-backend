<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{% if title is defined %}{{ title }}{% else %}Studio Perret{% endif %}{% endblock %}}</title>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
        {% block stylesheets %}
            {% for path in encore_entry_css_files('app') %}
                <link href="{{ absolute_url(path) }}" rel="stylesheet" />
            {% endfor %}
            {% for path in encore_entry_css_files('pdf') %}
                <link href="{{ absolute_url(path) }}" rel="stylesheet" />
            {% endfor %}
        {% endblock %}
    </head>
    <body>
    {% block body %}{% endblock %}
    </body>
</html>
